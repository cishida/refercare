import 'package:flutter/material.dart';

class W2sScreen extends StatelessWidget {
  const W2sScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('W2s');
  }
}
