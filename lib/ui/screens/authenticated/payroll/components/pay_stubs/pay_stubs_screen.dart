import 'package:flutter/material.dart';

class PayStubsScreen extends StatelessWidget {
  const PayStubsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Pay Stubs');
  }
}
