import 'package:flutter/material.dart';

class YapiScreen extends StatelessWidget {
  const YapiScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Yapi');
  }
}
