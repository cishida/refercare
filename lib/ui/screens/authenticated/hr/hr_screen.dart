import 'package:flutter/material.dart';

class HRScreen extends StatelessWidget {
  const HRScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('HR');
  }
}
